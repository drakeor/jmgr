#ifndef UPTIME_H
#define UPTIME_H

#include <iostream>
#include <fstream>
#include <cmath>
#include <string>

class Uptime {
public:
    // Method to load and return uptime as a string
    std::string getFormattedUptime();
};

#endif // UPTIME_H
